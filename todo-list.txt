- [x] Review current server startup flow (`main.py`, `src/` modules) to understand runtime requirements.
- [x] Identify environment variables and files that must be available inside the container.
- [ ] Determine runtime command (uvicorn vs python main) and ports to expose.
Analysis Phase:
- [x] Review current server startup flow (`main.py`, `src/` modules) to understand runtime requirements.
- [x] Identify environment variables and files that must be available inside the container.
- [x] Determine runtime command (use `uvicorn main:app`) and expose port 8000.
- [x] Review current server startup flow (`main.py`, `src/` modules) to understand runtime requirements.
- [x] Identify environment variables and files that must be available inside the container.
- [ ] Determine runtime command (uvicorn vs python main) and ports to expose.

Research Phase:
- [x] Compile Docker & FastAPI containerization best practices from latest sources (2025) gathered via web search.
- [x] Note recommendations on base images, non-root execution, caching strategy, and dependency installation order.

Implementation Phase:
- [x] Author a production-ready multi-stage `Dockerfile` using slim Python base, caching requirements, and non-root user.
- [x] Add a `.dockerignore` tuned for this repository to reduce build context.
- [x] Provide optional `docker-compose.yml` for local development convenience.
- [x] Update configuration/entrypoint logic if needed for container defaults.

Testing Phase:
- Validate Python dependencies by running `pytest` locally to ensure server still operates pre-containerization.
- (If Docker available) outline build/run commands; otherwise document manual verification steps.

Documentation Phase:
- Update `README.md` with Docker build/run instructions and compose usage.
- Mention new containerization artifacts and environment variable expectations.

---

Analysis Phase (Import Fixes):
- [ ] Identify modules inside `src/` still using old top-level imports (e.g., `from audio_processor import ...`).
- [ ] Confirm runtime entry points (`main.py`, tests) import from `src` package correctly.

Implementation Phase (Import Fixes):
- [ ] Convert intra-package imports to explicit relative form (e.g., `from .audio_processor import ...`).
- [ ] Update tests or helper scripts to import via package path (`from src...`).
- [ ] Ensure `PYTHONPATH` adjustments (if any) remain valid for Docker and local runs.

Testing Phase (Import Fixes):
- [ ] Run `python -m compileall src` or equivalent to catch syntax/import issues.
- [ ] Execute targeted unit tests (pytest) to verify imports succeed.

Documentation Phase (Import Fixes):
- [ ] Note import path change in README or developer notes if necessary.
